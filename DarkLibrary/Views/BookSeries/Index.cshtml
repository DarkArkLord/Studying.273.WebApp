@model IEnumerable<DataLayer.Entities.DBookSeries>

@{
    ViewData["Title"] = "Book series menu";
}

<div class="text-center">
    <h1 class="display-4">Book series menu</h1>
    
    <a class="nav-link text-dark" asp-area="" asp-controller="BookSeries" asp-action="Create">Create</a> <br />

    Count: @(Model.Count()) <br />

    @foreach(var item in Model)
    {
        <div>
            @($"{item.Id}) {item.Name}")
        </div>
    }
</div>
@model IEnumerable<DataLayer.Entities.DLibrarian>

@{
    ViewData["Title"] = "Librarian menu";
}

<div class="text-center">
    <h1 class="display-4">Librarian menu</h1>
    
    <a asp-area="" asp-controller="Librarian" asp-action="Create">Create</a> <br />

    Count: @(Model.Count()) <br />

    @foreach(var item in Model)
    {
        <div>
            @($"{item.Id}) {item.Name}")
        </div>
    }
</div>
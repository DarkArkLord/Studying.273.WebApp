@model IEnumerable<DataLayer.Entities.DClient>

@{
    ViewData["Title"] = "Client menu";
}

<div class="text-center">
    <h1 class="display-4">Client menu</h1>
    
    <a class="nav-link text-dark" asp-area="" asp-controller="Client" asp-action="Create">Create</a> <br />

    Count: @(Model.Count()) <br />

    @foreach(var item in Model)
    {
        <div>
            @($"{item.Id}) {item.Name}")
        </div>
    }
</div>